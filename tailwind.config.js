/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './views/**/*.ejs',       // EJS templates
    './public/**/*.html'      // Any static HTML files (if any)
  ],
  theme: {
    extend: {},
  },
  plugins: [],
}