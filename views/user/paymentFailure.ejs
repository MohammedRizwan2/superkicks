<!DOCTYPE html>
<html lang="en" class="h-full">
<head>
  <meta charset="UTF-8"/>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Payment Failed — Superkicks</title>
  
  <script src="https://cdn.tailwindcss.com"></script>
  <script>
    tailwind.config = {
      theme: {
        extend: {
          colors: {
            ash: '#f8f8f8',
            graphite: '#e5e5e5'
          }
        }
      }
    }
  </script>
  
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin/>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
  
  <style>
    html, body { font-family: Inter, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial; }
    .pulse-ring {
      animation: pulse-ring 1.5s ease-in-out infinite;
    }
    @keyframes pulse-ring {
      0% {
        transform: scale(.33);
      }
      80%, 100% {
        opacity: 0;
      }
    }
    .fade-in {
      animation: fadeIn 0.6s ease-in;
    }
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); }
      to { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body class="h-full bg-gradient-to-br from-red-50 to-orange-50">
  <%- include('../partials/header') %>

  <div class="min-h-screen flex items-center justify-center px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8 fade-in">
      <div class="text-center">
        <!-- Error Icon -->
        <div class="mx-auto flex items-center justify-center h-24 w-24 rounded-full bg-red-100 mb-6 relative">
          <div class="absolute inset-0 rounded-full bg-red-100 pulse-ring"></div>
          <i class="fas fa-times-circle text-red-600 text-4xl relative z-10"></i>
        </div>
        
        <!-- Main Heading -->
        <h1 class="text-3xl font-bold text-gray-900 mb-2">Payment Failed</h1>
        <p class="text-lg text-gray-600 mb-8">We couldn't process your payment</p>
        
        <!-- Error Details Card -->
        <div class="bg-white rounded-xl shadow-lg p-6 mb-8 border border-red-100">
          <div class="flex items-start space-x-3">
            <i class="fas fa-exclamation-triangle text-yellow-500 text-xl mt-1"></i>
            <div class="flex-1 text-left">
              <h3 class="text-sm font-semibold text-gray-900 mb-2">What went wrong?</h3>
              <p class="text-sm text-gray-600">
                <%= orderData.errorMessage || 'Your payment could not be processed at this time. This could be due to insufficient funds, network issues, or other technical problems.' %>
              </p>
              
              <% if (orderData.razorpayOrderId) { %>
                <div class="mt-4 p-3 bg-gray-50 rounded-lg">
                  <p class="text-xs text-gray-500">
                    <strong>Order ID:</strong> <%= orderData.razorpayOrderId %>
                  </p>
                  <% if (orderData.amount > 0) { %>
                    <p class="text-xs text-gray-500 mt-1">
                      <strong>Amount:</strong> ₹<%= orderData.amount.toLocaleString('en-IN') %>
                    </p>
                  <% } %>
                </div>
              <% } %>
            </div>
          </div>
        </div>

        <!-- Action Buttons -->
        <div class="space-y-4">
          <!-- Primary Action - Retry Payment -->
          <a href="/user/orders" 
             class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-lg text-white bg-black hover:bg-gray-800 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition duration-200 transform hover:scale-105">
            <span class="absolute left-0 inset-y-0 flex items-center pl-3">
              <i class="fas fa-redo text-white group-hover:text-gray-200"></i>
            </span>
            Try Payment Again
          </a>

          <!-- Secondary Actions -->
          <div class="flex space-x-4">
            <a href="/user/cart" 
               class="flex-1 inline-flex justify-center items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition duration-200">
              <i class="fas fa-shopping-cart mr-2"></i>
              Back to Cart
            </a>
            
            <a href="/user/orders" 
               class="flex-1 inline-flex justify-center items-center px-4 py-2 border border-gray-300 shadow-sm text-sm font-medium rounded-lg text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition duration-200">
              <i class="fas fa-list mr-2"></i>
              My Orders
            </a>
          </div>
        </div>

        <!-- Help Section -->
        <div class="mt-8 p-6 bg-blue-50 rounded-xl border border-blue-200">
          <h3 class="text-sm font-semibold text-blue-900 mb-2">
            <i class="fas fa-info-circle mr-2"></i>
            Need Help?
          </h3>
          <div class="text-sm text-blue-800 space-y-2">
            <p>• Check your card details and try again</p>
            <p>• Ensure you have sufficient balance</p>
            <p>• Try using a different payment method</p>
            <p>• Contact your bank if the issue persists</p>
          </div>
          
          <div class="mt-4 pt-4 border-t border-blue-200">
            <p class="text-xs text-blue-700">
              Still having trouble? 
              <a href="mailto:support@superkicks.com" class="font-medium underline hover:text-blue-900">
                Contact our support team
              </a>
            </p>
          </div>
        </div>

        <!-- Payment Methods Info -->
        <div class="mt-6 text-center">
          <p class="text-xs text-gray-500 mb-3">We accept:</p>
          <div class="flex justify-center space-x-4 text-gray-400">
            <i class="fab fa-cc-visa text-2xl"></i>
            <i class="fab fa-cc-mastercard text-2xl"></i>
            <i class="fab fa-cc-amex text-2xl"></i>
            <i class="fas fa-university text-2xl"></i>
            <i class="fas fa-mobile-alt text-2xl"></i>
            <i class="fas fa-wallet text-2xl"></i>
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Auto-refresh notice (optional) -->
  <% if (orderData.paymentMethod === 'RAZORPAY') { %>
    <div class="fixed bottom-4 right-4 bg-white rounded-lg shadow-lg p-4 border border-gray-200 max-w-sm">
      <div class="flex items-start space-x-3">
        <i class="fas fa-clock text-blue-500 text-sm mt-0.5"></i>
        <div>
          <p class="text-xs font-medium text-gray-900">Payment Session</p>
          <p class="text-xs text-gray-600">You can retry the same payment within 15 minutes</p>
        </div>
      </div>
    </div>
  <% } %>

 
</body>
</html>
