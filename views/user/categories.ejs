<!DOCTYPE html>
<html lang="en" class="scroll-smooth antialiased">
<head>
  <meta charset="UTF-8">
  <title>Categories - Superkicks</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <script src="https://cdn.tailwindcss.com"></script>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.12.5/gsap.min.js"></script>
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css"/>
</head>
<body class="bg-white text-gray-900 font-sans">

  <!-- ░░ MODERN BLACK NAVBAR ░░ -->
  <header class="sticky top-0 z-50 bg-black/95 backdrop-blur supports-[backdrop-filter]:bg-black/70">
    <nav class="mx-auto max-w-7xl flex items-center justify-between px-6 py-4">
      <a href="/" class="text-2xl font-extrabold tracking-[0.15em] text-white hover:text-gray-300 transition">
        SUPERKICKS
      </a>

      <ul class="hidden md:flex items-center gap-10 text-sm uppercase font-medium">
        <li><a href="/" class="text-white hover:text-gray-300">Home</a></li>
        <li><a href="/user/product/list" class="text-white hover:text-gray-300">Shop</a></li>
        <li><a href="/user/categories" class="text-gray-300 border-b border-gray-300">Categories</a></li>

        <% if (user) { %>
          <li class="flex items-center gap-4">
            <span class="text-white/80"><i class="fa-solid fa-user mr-1"></i><%= user.fullName || user.email %></span>
            <a href="/user/logout" class="px-4 py-1.5 rounded border border-white text-white hover:bg-white hover:text-black transition">
              Logout
            </a>
          </li>
        <% } else { %>
          <li class="space-x-2">
            <a href="/user/login" class="px-4 py-1.5 rounded border border-white text-white hover:bg-white hover:text-black transition">
              Login
            </a>
            <a href="/user/signup" class="px-4 py-1.5 rounded bg-white text-black hover:bg-transparent hover:text-white border border-white transition">
              Sign Up
            </a>
          </li>
        <% } %>
      </ul>

      <button id="mobileBtn" class="md:hidden text-white text-2xl focus:outline-none">
        <i class="fa-solid fa-bars"></i>
      </button>
    </nav>

    <!-- Mobile Menu -->
    <div id="mobileMenu" class="fixed inset-0 z-40 bg-black/90 backdrop-blur-md translate-x-full transition-transform duration-300 md:hidden">
      <div class="flex flex-col items-center gap-8 pt-24 text-lg font-medium">
        <a href="/" class="text-white hover:text-gray-300">Home</a>
        <a href="/user/product/list" class="text-white hover:text-gray-300">Shop</a>
        <a href="/user/categories" class="text-gray-300">Categories</a>
        <% if (user) { %>
          <span class="text-white/80"><%= user.fullName || user.email %></span>
          <a href="/user/logout" class="px-6 py-2 rounded border border-white text-white hover:bg-white hover:text-black transition">Logout</a>
        <% } else { %>
          <a href="/user/login" class="px-6 py-2 rounded border border-white text-white hover:bg-white hover:text-black transition">Login</a>
          <a href="/user/signup" class="px-6 py-2 rounded bg-white text-black hover:bg-transparent hover:text-white border border-white transition">Sign Up</a>
        <% } %>
      </div>
    </div>
  </header>

  <!-- ░░ HERO SECTION ░░ -->
  <section class="bg-gray-900 text-white py-20">
    <div class="max-w-7xl mx-auto px-6 text-center">
      <h1 class="text-4xl lg:text-5xl font-bold mb-4">Shop by Category</h1>
      <p class="text-xl text-gray-300 max-w-2xl mx-auto mb-8">
        Discover our complete range of footwear categories, from athletic shoes to casual wear
      </p>
      <p class="text-sm text-gray-400"><%= totalCategories %> categories available</p>
    </div>
  </section>

  <!-- ░░ FEATURED CATEGORIES ░░ -->
  <% if (featuredCategories && featuredCategories.length > 0) { %>
    <section class="max-w-7xl mx-auto px-6 py-16">
      <h2 class="text-3xl font-bold mb-8 text-center">Popular Categories</h2>
      <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
        <% featuredCategories.forEach(category => { %>
          <a href="/user/categories/<%= category._id %>" class="group block" data-gsap="featured-card">
            <div class="bg-gray-900 text-white rounded-lg overflow-hidden transition hover:-translate-y-2 hover:shadow-2xl">
              <div class="aspect-square bg-gradient-to-br from-gray-800 to-gray-900 flex items-center justify-center">
                <i class="fa-solid fa-shoe-prints text-6xl text-white/60 group-hover:text-white transition"></i>
              </div>
              <div class="p-6">
                <h3 class="text-xl font-bold mb-2 group-hover:text-gray-300"><%= category.name %></h3>
                <% if (category.description) { %>
                  <p class="text-gray-400 text-sm mb-3 line-clamp-2"><%= category.description %></p>
                <% } %>
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-400"><%= category.productCount %> products</span>
                  <i class="fa-solid fa-arrow-right text-sm group-hover:translate-x-1 transition"></i>
                </div>
              </div>
            </div>
          </a>
        <% }) %>
      </div>
    </section>
  <% } %>

  <!-- ░░ SEARCH & FILTER ░░ -->
  <main class="max-w-7xl mx-auto px-6 py-16">
    <!-- Search Form -->
    <form id="searchForm" class="mb-12 max-w-md mx-auto">
      <div class="relative">
        <input type="text" name="q" id="searchInput" 
               placeholder="Search categories..." 
               value="<%= q %>"
               class="w-full pl-12 pr-4 py-4 rounded-lg border border-gray-300 focus:outline-none focus:ring-2 focus:ring-gray-900 text-lg">
        <i class="fa-solid fa-search absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400"></i>
        <% if (q) { %>
          <button type="button" onclick="clearSearch()" 
                  class="absolute right-4 top-1/2 transform -translate-y-1/2 text-gray-400 hover:text-gray-600">
            <i class="fa-solid fa-times"></i>
          </button>
        <% } %>
      </div>
    </form>

    <!-- ░░ CATEGORIES GRID ░░ -->
    <div id="categoriesContainer" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-8">
      <% if (!categories || categories.length === 0) { %>
        <div class="col-span-full text-center py-16">
          <i class="fa-regular fa-folder-open text-6xl text-gray-400 mb-4"></i>
          <h3 class="text-xl font-semibold text-gray-700 mb-2">No Categories Found</h3>
          <p class="text-gray-500">
            <% if (q) { %>
              No categories match your search "<%= q %>"
            <% } else { %>
              No categories are currently available
            <% } %>
          </p>
        </div>
      <% } else { %>
        <% categories.forEach(category => { %>
          <a href="/user/categories/<%= category._id %>" class="group block" data-gsap="category-card">
            <div class="bg-gray-50 rounded-lg overflow-hidden transition hover:-translate-y-1 hover:shadow-lg">
              <div class="aspect-square bg-gradient-to-br from-gray-100 to-gray-200 flex items-center justify-center group-hover:from-gray-900 group-hover:to-gray-800 transition-all duration-300">
                <i class="fa-solid fa-shoe-prints text-5xl text-gray-600 group-hover:text-white transition-all duration-300"></i>
              </div>
              <div class="p-6">
                <h3 class="text-lg font-bold mb-2 group-hover:text-gray-600 transition"><%= category.name %></h3>
                <% if (category.description) { %>
                  <p class="text-gray-600 text-sm mb-3 line-clamp-2"><%= category.description %></p>
                <% } %>
                <div class="flex items-center justify-between">
                  <span class="text-sm text-gray-500">
                    <i class="fa-solid fa-box mr-1"></i><%= category.productCount %> products
                  </span>
                  <i class="fa-solid fa-arrow-right text-sm text-gray-400 group-hover:translate-x-1 transition"></i>
                </div>
              </div>
            </div>
          </a>
        <% }) %>
      <% } %>
    </div>

    <!-- ░░ PAGINATION ░░ -->
    <% if (totalPages > 1) { %>
      <div id="paginationContainer" class="mt-12 flex justify-center items-center gap-4">
        <% if (hasPrevPage) { %>
          <a href="#" data-page="<%= currentPage - 1 %>" class="page-link px-6 py-3 rounded border hover:bg-gray-50 transition">
            <i class="fa-solid fa-arrow-left mr-2"></i>Previous
          </a>
        <% } %>
        <span class="px-6 py-3 bg-gray-900 text-white rounded font-semibold">
          <%= currentPage %> / <%= totalPages %>
        </span>
        <% if (hasNextPage) { %>
          <a href="#" data-page="<%= currentPage + 1 %>" class="page-link px-6 py-3 rounded border hover:bg-gray-50 transition">
            Next<i class="fa-solid fa-arrow-right ml-2"></i>
          </a>
        <% } %>
      </div>
    <% } %>
  </main>

  <!-- ░░ FOOTER ░░ -->
  <footer class="bg-gray-900 text-gray-200 px-6 py-12">
    <div class="max-w-7xl mx-auto grid sm:grid-cols-2 lg:grid-cols-3 gap-10 text-sm">
      <div>
        <h4 class="font-semibold mb-3">SUPERKICKS</h4>
        <p class="opacity-70">© <%= new Date().getFullYear() %>. All Rights Reserved.</p>
      </div>
      <div class="space-y-2">
        <h4 class="font-semibold mb-3">Company</h4>
        <a href="/privacy" class="block hover:opacity-80">Privacy Policy</a>
        <a href="/terms" class="block hover:opacity-80">Terms of Service</a>
        <a href="/contact" class="block hover:opacity-80">Contact</a>
      </div>
      <div class="space-y-2">
        <h4 class="font-semibold mb-3">Follow Us</h4>
        <div class="flex gap-4 text-lg">
          <a href="#" class="hover:text-white"><i class="fab fa-instagram"></i></a>
          <a href="#" class="hover:text-white"><i class="fab fa-facebook-f"></i></a>
          <a href="#" class="hover:text-white"><i class="fab fa-x-twitter"></i></a>
        </div>
      </div>
    </div>
  </footer>

  <!-- ░░ SCRIPTS ░░ -->
  <script src="https://cdn.jsdelivr.net/npm/axios
